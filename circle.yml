machine:
  node:
    version: 0.10.34
  services:
    - docker
dependencies:
  override:
    - npm install
    - npm run build
    - docker build -t "freethenation/viewshed:${CIRCLE_BUILD_NUM}" .
test:
  override:
    - echo skipping tests