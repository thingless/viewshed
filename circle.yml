machine:
  node:
    version: 0.10.33
  services:
    - docker
dependencies:
  override:
    - npm install --dev
    - npm run build
    - docker build -t "freethenation/viewshed:${CIRCLE_BUILD_NUM}"  --no-cache .
test:
  override:
    - echo skipping tests